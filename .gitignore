## Project gitignore

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Build outputs / compiled binaries
/bin/
# Dist output (local bundles & artifacts)
/dist/*
!/dist/README.md
!/dist/.gitkeep
/sb29guard
sb29guard.exe
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Go workspace files (if locally used but not committed)
go.work
go.work.sum

# DNS / manifest outputs
dist/dns/
**/manifest.json
# Generally ignore PowerShell scripts, except curated docs scripts
**/*.ps1
!docs/implementers/scripts/*.ps1
nginx-blocked.conf

# Aggregated logs & runtime logs
logs/

# Policy cache / sheet sync cache
cache/
policy/domains.generated.yaml

# Local environment & secrets
.env
.env.*
!.env.example
secrets/

# Test coverage / tooling
coverage.out
coverage/
*.coverprofile
*.cov
*.lcov

# Profiling & trace artifacts
cpu.prof
mem.prof
block.prof
mutex.prof
trace.out

# Logs
*.log

# Temporary files
*.tmp
*.swp
*.swo
*.bak
*.orig
*~

# Shell history / direnv / local dev helpers
.envrc
.direnv/

# Vendor (if a developer runs `go mod vendor` locally)
vendor/
