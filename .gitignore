# Easy-mode local state
/easy-mode/.env
/easy-mode/out/
/easy-mode/policy/

# Editor/OS
.DS_Store
Thumbs.db

# Go build/test caches (optional local)
/dist/# SB29-guard Generated / Build Artifacts

# Build outputs / compiled binaries
/.gitignore
/bin/
/dist/
/sb29guard
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Go workspace files (if locally used but not committed)
go.work
go.work.sum

# DNS / manifest outputs
dist/dns/
**/manifest.json

# Aggregated logs & runtime logs
logs/

# Policy cache / sheet sync cache
cache/
policy/domains.generated.yaml

# Local environment & secrets
.env
.env.*
!.env.example
secrets/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Test coverage / tooling
coverage.out
coverage/
*.coverprofile
*.cov
*.lcov

# Profiling & trace artifacts
cpu.prof
mem.prof
block.prof
mutex.prof
trace.out

# Logs
*.log

# Temporary files
*.tmp
*.swp
*.swo
*.bak
*.orig
*~

# Shell history / direnv / local dev helpers
.envrc
.direnv/

# Vendor (if a developer runs `go mod vendor` locally)
vendor/
